services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: hyperfin
      POSTGRES_USER: hyperfin
      POSTGRES_PASSWORD: hyperfin
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
